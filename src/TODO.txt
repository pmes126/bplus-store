NodeView + page.

TODO
child_ptr_at(idx) Done
get_child_for_key(key) Done

replace_child_at(idx)

delete_at(idx) Done

insert_entry_at(idx, entry) TODO
insert_entry(entry) Done

delte_key_at(key)

TODO: Verify
read_value_at(idx) Looks good
insert_encoded(k, v)
